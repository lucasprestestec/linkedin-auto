// Compartilhado entre client (formulário de busca) e server (paginação da busca) —
// sem dependências de servidor pra poder ser importado num client component.
export const MAX_SEARCH_RESULTS = 50;
